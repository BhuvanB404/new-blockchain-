# .env.example - Copy this to .env and fill in your values

# Server Configuration
NODE_ENV=development
PORT=5000
APP_VERSION=2.0.0

# JWT Configuration (IMPORTANT: Generate secure random strings for production)
JWT_SECRET=5175916f70346afb88d848a40fbd2397e0789cd8321ea7af55ef653e43bd7ea0
JWT_REFRESH_SECRET=5175916f70346afb88d848a40fbd2397e0789cd8321ea7af55ef653e43bd7ea0
JWT_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Security Configuration
BCRYPT_ROUNDS=12
PASSWORD_MIN_LENGTH=8
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=1800000

# Database Configuration (if using persistent storage)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ayurveda_auth
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=10
LOGIN_RATE_LIMIT_MAX=5

# Email Configuration (for future email verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Redis Configuration (for production session storage)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# Hyperledger Fabric Configuration
FABRIC_NETWORK_PATH=../fabric-samples/test-network
WALLET_PATH=./wallet
CHANNEL_NAME=mychannel
CHAINCODE_NAME=ehrChainCode

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads
ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf,doc,docx

# Logging Configuration  
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Monitoring (optional)
ENABLE_METRICS=false
METRICS_PORT=9090

# Development Configuration
ENABLE_SWAGGER=true
SWAGGER_PATH=/api/docs
